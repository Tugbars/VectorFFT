# prototype cost_model — DAG-FFT-compiler edition

ESTIMATE backend for the DAG-compiled codelets in `src/prototype/codelets/`.

## What this is for

The cost model serves one specific user persona: **"I want a good
plan, I want it in microseconds, I don't want to measure, I don't want
to persist wisdom."** That user calls `vfft_plan_*(N, K, VFFT_ESTIMATE)`,
gets a plan picked by closed-form scoring, runs it, done. Per
`docs/cost_model/00_thesis.md`: ~µs plan time, ~1.2× of measured
quality, hardware-portable via per-host CPE regeneration.

The cost model is **not** in the MEASURE path. MEASURE-mode users
time candidate factorizations end-to-end and persist the empirical
winner to wisdom; they don't consult the cost model at all. The two
modes are independent code paths serving different user personas.

## Why a separate tree

The production cost model is calibrated against codelets emitted by the
Python generators (`tools/radix_profile/extract.py` parses their C
source; `tools/radix_profile/measure_cpe.c` times them). Those two
generator families produce **structurally different code** even when
they compute the same FFT:

- Different SSA / hash-cons → different intermediate counts
- Different scheduler choice → different instruction order
- Different register allocation: production uses gcc-RA; prototype
  M-active uses chordal coloring + `register asm()` pinning + barriers
- M-active codelets are 5–23% faster per M_PROJECT.md §4 — production
  CPE numbers don't reflect that

A single shared `radix_cpe.h` would corrupt the cost model: the same
table would describe different code depending on which generator the
operator last ran. The fix is two parallel tables.

## File map

| File | Origin | Purpose |
|---|---|---|
| `measure_cpe.c` | `tools/radix_profile/measure_cpe.c` | Dynamic CPE timing — retarget to prototype codelet symbol names + paths |
| `extract.py` | `tools/radix_profile/extract.py` | Static op-count extractor — **planned replacement** with `extract.ml` that reads the hash-consed DAG directly (no regex over emitted C) |
| `factorizer.h` | `src/core/factorizer.h` | Cost formula — consume the prototype CPE table; otherwise structurally identical (memory-bound thesis is hardware-property, not codegen-property) |
| `generated/radix_profile.h` | `src/core/generated/radix_profile.h` | Op counts. Initial copy is the production header; will be regenerated by `extract.ml` once that lands |
| `generated/radix_cpe.h` | `src/core/generated/radix_cpe.h` | CPE numbers. Initial copy is the production header; needs re-measurement against prototype codelets |
| `TOOLS_README.md` | `tools/radix_profile/README.md` | Reference doc from the production tools/ tree |

## Scope

**In scope for v1:**

- 1D c2c codelets at the production-target subset: Radix R × {avx512, avx2}
- **Single per-radix CPE number** (not per-variant). The production CPE
  table has separate columns for cyc_n1 / cyc_t1 / cyc_t1s / cyc_log3
  because the old executor consulted per-stage variant predicates
  (`wisdom_bridge.h`). Those predicates have been retired: per-stage
  variant decisions need cross-stage state they can't see, so they
  weren't actually picking variants correctly. For ESTIMATE mode the
  cost model now uses a single per-R cycle estimate; variant choice
  is the codelet generator's concern, not the planner's.
- Algorithm-class CSE (Direct conjugate-pair for odd primes;
  CT share_subsums for pow2/composites) is captured implicitly per-R —
  the cycle cost reflects whichever pipeline produced that radix's codelet.
- Emission style fixed to **M-active** (`VFFT_USE_REGALLOC=1 VFFT_USE_REGALLOC_M5=1`).
  Falling back to gcc-RA is a debugging mode, not a deployment mode.
- Scheduler fixed to whatever production-target schedule the prototype
  emits today.
- DIT-fwd direction (DIF, bwd are symmetric in cycles).

**Out of scope for v1 (future v2+):**

- Strided codelets (Design C 2D row FFT) — separate code path, separate
  CPE concerns
- Twidsq codelets — separate code path
- Trig family (DCT-II/III/IV, DST-II/III, DHT) — separate code path,
  would need its own table
- R2C / C2R / RDFT / HC2HC / HC2C — separate code path

## Fingerprint extension

Production's `radix_cpe.h` fingerprint includes host OS / CPU / effective
freq / max CV / date. The prototype version should additionally document:

- **Algorithm class per radix.** E.g. `R=11: Direct/conjugate-pair`,
  `R=16: CT(4,4) + share_subsums`, `R=32: CT(4,8) + share_subsums`.
  Tells readers which CSE pipeline produced each row.
- **Emission mode.** `M-active (VFFT_USE_REGALLOC=1 VFFT_USE_REGALLOC_M5=1)`.
- **Scheduler.** Whichever production-target scheduler was used.

That way a future reader of a stale or wrong-config CPE table can spot
the issue in code review without running the codelets.

## Static op-count extraction

The production `extract.py` parses C source via regex because the Python
generators emit C as their primary output. **The OCaml DAG compiler
doesn't need this.** We already have the hash-consed `Algsimp.t` DAG
before emission — counting `NK_Add` / `NK_Mul` / `NK_Fma` / `NK_Load` /
`NK_CmulRe` / `NK_CmulIm` reachable from the assignment roots is a
~20-line `count_ops : Algsimp.t list -> op_counts` function on the
existing structures.

Planned replacement: `extract.ml` that runs inside the OCaml pipeline,
emits `generated/radix_profile.h` from the DAG. The current
`extract.py` is kept as a transitional reference; once `extract.ml`
lands and produces a matching-format header, `extract.py` can be
removed from this tree.

## Workflow (planned)

```bash
# 1. Regenerate codelets with M-active
cd src/prototype
VFFT_USE_REGALLOC=1 VFFT_USE_REGALLOC_M5=1 \
  ISA=both ./scripts/generate_codelets.sh

# 2. Generate static op-count profile from the DAG (after extract.ml lands)
dune exec cost_model/extract.exe -- --isa avx2   > cost_model/generated/radix_profile.h
dune exec cost_model/extract.exe -- --isa avx512 >> cost_model/generated/radix_profile.h

# 3. Build measure_cpe against prototype codelets
gcc-11 -O3 -mavx2 -mfma -flive-range-shrinkage \
  cost_model/measure_cpe.c codelets/avx2/**/*.c \
  -o cost_model/measure_cpe.exe

# 4. Measure CPE on a quiet host
cost_model/measure_cpe.exe                # variance check enforced
cost_model/measure_cpe.exe --force        # override if needed
```

The variance check, RDTSC-based frequency calibration, K=256 baseline,
and 5% CV refusal threshold are inherited unchanged from production.
Those are methodology choices about how to measure cleanly, not
choices about what to measure — they transfer cleanly.

## What we will edit

The copies are starting points. Concrete diffs we'll need to make:

- **`measure_cpe.c`** — function symbol names change from
  `radix{N}_t1_dit_fwd_avx{2,512}` to
  `radix{N}_t1_dit_fwd_avx{2,512}_gen_inplace_su_spill` (prototype
  naming); codelet `.c` paths change from
  `src/vectorfft_tune/generated/r{N}/` to `src/prototype/codelets/{isa}/{family}/`.
  Header path changes to `src/prototype/cost_model/generated/radix_cpe.h`.
  CPE struct collapses from 4 columns (cyc_n1 / cyc_t1 / cyc_t1s /
  cyc_log3) to 1 (cyc).
- **`extract.py`** — replace with `extract.ml` operating on the DAG.
- **`factorizer.h`** — include path change to point at prototype's
  `radix_cpe.h` and `radix_profile.h`; drop variant-predicate calls
  (the old `stride_prefer_t1s` / `stride_prefer_dit_log3` paths) and
  use the single per-R cycle directly. Closed-form, no executor coupling.
- **`generated/radix_cpe.h`** — regenerate from prototype codelets,
  single-column.
- **`generated/radix_profile.h`** — regenerate from DAG.

`wisdom_bridge.h` is intentionally absent — see the §Scope note above.

## Reference

The production cost-model docs at `docs/cost_model/` (00_thesis through
09_decisions) cover the architecture and methodology in detail. Read
those first for the "why"; this tree is the prototype-specific "what".
