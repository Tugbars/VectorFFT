/**
 * @file fft_radix3_scalar_ct_n1_scaled.h
 * @brief DFT-3 SCALAR n1_scaled (separate is/os, output *= scale)
 *
 * FFTW-style codelet for recursive CT executor.
 * Generated by gen_radix3.py --variant ct_n1_scaled
 */

#ifndef FFT_RADIX3_SCALAR_CT_CT_N1_SCALED_H
#define FFT_RADIX3_SCALAR_CT_CT_N1_SCALED_H

#ifndef R3S_CT_LD
#define R3S_CT_LD(p) (*(p))
#define R3S_CT_ST(p,v) (*(p)=(v))
#endif
#define LD R3S_CT_LD
#define ST R3S_CT_ST

static void
radix3_n1_scaled_fwd_scalar(
    const double * __restrict__ in_re, const double * __restrict__ in_im,
    double * __restrict__ out_re, double * __restrict__ out_im,
    size_t is, size_t os, size_t vl, double scale)
{
    const double KHALF = 0.500000000000000000000000000000000000000000000;
    const double KS    = 0.866025403784438596588302061718422919511795044;

    double x0_re,x0_im,x1_re,x1_im,x2_re,x2_im;

    for (size_t k = 0; k < vl; k++) {
        x0_re = in_re[0*is+k]; x0_im = in_im[0*is+k];
        x1_re = in_re[1*is+k]; x1_im = in_im[1*is+k];
        x2_re = in_re[2*is+k]; x2_im = in_im[2*is+k];

        /* DFT-3 butterfly [fwd] */
        double ar=(x1_re)+(x2_re), ai=(x1_im)+(x2_im);
        double dr=(x1_re)-(x2_re), di=(x1_im)-(x2_im);
        double R1r=(x0_re)-(KHALF)*(ar), R1i=(x0_im)-(KHALF)*(ai);
        x0_re=(x0_re)+(ar); x0_im=(x0_im)+(ai);
        x1_re=(KS)*(di)+(R1r); x1_im=(R1i)-(KS)*(dr);
        x2_re=(R1r)-(KS)*(di); x2_im=(KS)*(dr)+(R1i);

        out_re[0*os+k] = scale * x0_re; out_im[0*os+k] = scale * x0_im;
        out_re[1*os+k] = scale * x1_re; out_im[1*os+k] = scale * x1_im;
        out_re[2*os+k] = scale * x2_re; out_im[2*os+k] = scale * x2_im;
    }
}

static void
radix3_n1_scaled_bwd_scalar(
    const double * __restrict__ in_re, const double * __restrict__ in_im,
    double * __restrict__ out_re, double * __restrict__ out_im,
    size_t is, size_t os, size_t vl, double scale)
{
    const double KHALF = 0.500000000000000000000000000000000000000000000;
    const double KS    = 0.866025403784438596588302061718422919511795044;

    double x0_re,x0_im,x1_re,x1_im,x2_re,x2_im;

    for (size_t k = 0; k < vl; k++) {
        x0_re = in_re[0*is+k]; x0_im = in_im[0*is+k];
        x1_re = in_re[1*is+k]; x1_im = in_im[1*is+k];
        x2_re = in_re[2*is+k]; x2_im = in_im[2*is+k];

        /* DFT-3 butterfly [bwd] */
        double ar=(x1_re)+(x2_re), ai=(x1_im)+(x2_im);
        double dr=(x1_re)-(x2_re), di=(x1_im)-(x2_im);
        double R1r=(x0_re)-(KHALF)*(ar), R1i=(x0_im)-(KHALF)*(ai);
        x0_re=(x0_re)+(ar); x0_im=(x0_im)+(ai);
        x2_re=(KS)*(di)+(R1r); x2_im=(R1i)-(KS)*(dr);
        x1_re=(R1r)-(KS)*(di); x1_im=(KS)*(dr)+(R1i);

        out_re[0*os+k] = scale * x0_re; out_im[0*os+k] = scale * x0_im;
        out_re[1*os+k] = scale * x1_re; out_im[1*os+k] = scale * x1_im;
        out_re[2*os+k] = scale * x2_re; out_im[2*os+k] = scale * x2_im;
    }
}


#undef LD
#undef ST

#endif /* FFT_RADIX3_SCALAR_CT_CT_N1_SCALED_H */
