/**
 * @file fft_radix2_scalar.h
 * @brief DFT-2 SCALAR codelets
 *
 * t2 codelets: loop over K, elements at stride K
 *   notw/tw_dit/tw_dif (K parameter = stride = loop bound)
 * CT codelets: n1 (stride is/os), t1_dit, t1_dif, n1_ovs
 *
 * Generated by gen_radix2.py
 */

#ifndef FFT_RADIX2_SCALAR_H
#define FFT_RADIX2_SCALAR_H

#include <stddef.h>

/* === t2 codelets: loop over K, elements at stride K === */

static inline void
radix2_notw_kernel_fwd_scalar(
    const double * __restrict__ in_re, const double * __restrict__ in_im,
    double * __restrict__ out_re, double * __restrict__ out_im,
    size_t K)
{
    for (size_t k = 0; k < K; k++) {
        double ar = in_re[k], ai = in_im[k];
        double br = in_re[K+k], bi = in_im[K+k];
        out_re[k]   = (ar)+(br);
        out_im[k]   = (ai)+(bi);
        out_re[K+k] = (ar)-(br);
        out_im[K+k] = (ai)-(bi);
    }
}

static inline void
radix2_notw_kernel_bwd_scalar(
    const double * __restrict__ in_re, const double * __restrict__ in_im,
    double * __restrict__ out_re, double * __restrict__ out_im,
    size_t K)
{
    for (size_t k = 0; k < K; k++) {
        double ar = in_re[k], ai = in_im[k];
        double br = in_re[K+k], bi = in_im[K+k];
        out_re[k]   = (ar)+(br);
        out_im[k]   = (ai)+(bi);
        out_re[K+k] = (ar)-(br);
        out_im[K+k] = (ai)-(bi);
    }
}

static inline void
radix2_tw_dit_kernel_fwd_scalar(
    const double * __restrict__ in_re, const double * __restrict__ in_im,
    double * __restrict__ out_re, double * __restrict__ out_im,
    const double * __restrict__ tw_re, const double * __restrict__ tw_im,
    size_t K)
{
    for (size_t k = 0; k < K; k++) {
        double ar = in_re[k], ai = in_im[k];
        double br = in_re[K+k], bi = in_im[K+k];
        double wr = tw_re[k], wi = tw_im[k];
        double tr = br*wr - bi*wi;
        double ti = br*wi + bi*wr;
        out_re[k]   = ar + tr;
        out_im[k]   = ai + ti;
        out_re[K+k] = ar - tr;
        out_im[K+k] = ai - ti;
    }
}

static inline void
radix2_tw_dit_kernel_bwd_scalar(
    const double * __restrict__ in_re, const double * __restrict__ in_im,
    double * __restrict__ out_re, double * __restrict__ out_im,
    const double * __restrict__ tw_re, const double * __restrict__ tw_im,
    size_t K)
{
    for (size_t k = 0; k < K; k++) {
        double ar = in_re[k], ai = in_im[k];
        double br = in_re[K+k], bi = in_im[K+k];
        double wr = tw_re[k], wi = tw_im[k];
        double tr = br*wr + bi*wi;
        double ti = bi*wr - br*wi;
        out_re[k]   = ar + tr;
        out_im[k]   = ai + ti;
        out_re[K+k] = ar - tr;
        out_im[K+k] = ai - ti;
    }
}

static inline void
radix2_tw_dif_kernel_fwd_scalar(
    const double * __restrict__ in_re, const double * __restrict__ in_im,
    double * __restrict__ out_re, double * __restrict__ out_im,
    const double * __restrict__ tw_re, const double * __restrict__ tw_im,
    size_t K)
{
    for (size_t k = 0; k < K; k++) {
        double ar = in_re[k], ai = in_im[k];
        double br = in_re[K+k], bi = in_im[K+k];
        double sr = ar + br, si = ai + bi;
        double dr = ar - br, di = ai - bi;
        double wr = tw_re[k], wi = tw_im[k];
        out_re[k]   = sr;
        out_im[k]   = si;
        out_re[K+k] = dr*wr - di*wi;
        out_im[K+k] = dr*wi + di*wr;
    }
}

static inline void
radix2_tw_dif_kernel_bwd_scalar(
    const double * __restrict__ in_re, const double * __restrict__ in_im,
    double * __restrict__ out_re, double * __restrict__ out_im,
    const double * __restrict__ tw_re, const double * __restrict__ tw_im,
    size_t K)
{
    for (size_t k = 0; k < K; k++) {
        double ar = in_re[k], ai = in_im[k];
        double br = in_re[K+k], bi = in_im[K+k];
        double sr = ar + br, si = ai + bi;
        double dr = ar - br, di = ai - bi;
        double wr = tw_re[k], wi = tw_im[k];
        out_re[k]   = sr;
        out_im[k]   = si;
        out_re[K+k] = dr*wr + di*wi;
        out_im[K+k] = di*wr - dr*wi;
    }
}

/* === CT codelets: stride-based n1 + t1 for executor === */

static inline void
radix2_t1_dit_fwd(
    double * __restrict__ rio_re, double * __restrict__ rio_im,
    const double * __restrict__ W_re, const double * __restrict__ W_im,
    size_t ios, size_t mb, size_t me, size_t ms)
{
    for (size_t m = mb; m < me; m++) {
        const double x0r = rio_re[m*ms + 0*ios], x0i = rio_im[m*ms + 0*ios];
        const double r1r = rio_re[m*ms + 1*ios], r1i = rio_im[m*ms + 1*ios];
        const double wr = W_re[0*me + m], wi = W_im[0*me + m];
        const double x1r = r1r*wr - r1i*wi, x1i = r1r*wi + r1i*wr;
        rio_re[m*ms + 0*ios] = x0r + x1r; rio_im[m*ms + 0*ios] = x0i + x1i;
        rio_re[m*ms + 1*ios] = x0r - x1r; rio_im[m*ms + 1*ios] = x0i - x1i;
    }
}

static inline void
radix2_t1_dif_fwd(
    double * __restrict__ rio_re, double * __restrict__ rio_im,
    const double * __restrict__ W_re, const double * __restrict__ W_im,
    size_t ios, size_t mb, size_t me, size_t ms)
{
    for (size_t m = mb; m < me; m++) {
        const double x0r = rio_re[m*ms + 0*ios], x0i = rio_im[m*ms + 0*ios];
        const double x1r = rio_re[m*ms + 1*ios], x1i = rio_im[m*ms + 1*ios];
        const double wr = W_re[0*me + m], wi = W_im[0*me + m];
        const double dr = x0r - x1r, di = x0i - x1i;
        rio_re[m*ms + 0*ios] = x0r + x1r; rio_im[m*ms + 0*ios] = x0i + x1i;
        rio_re[m*ms + 1*ios] = dr*wr - di*wi; rio_im[m*ms + 1*ios] = dr*wi + di*wr;
    }
}

static inline void
radix2_t1_oop_dit_fwd_scalar(
    const double * __restrict__ in_re, const double * __restrict__ in_im,
    double * __restrict__ out_re, double * __restrict__ out_im,
    const double * __restrict__ W_re, const double * __restrict__ W_im,
    size_t is, size_t os, size_t me)
{
    for (size_t m = 0; m < me; m++) {
        const double x0r = in_re[m + 0*is], x0i = in_im[m + 0*is];
        const double r1r = in_re[m + 1*is], r1i = in_im[m + 1*is];
        const double wr = W_re[0*me + m], wi = W_im[0*me + m];
        const double x1r = r1r*wr - r1i*wi, x1i = r1r*wi + r1i*wr;
        out_re[m + 0*os] = x0r + x1r; out_im[m + 0*os] = x0i + x1i;
        out_re[m + 1*os] = x0r - x1r; out_im[m + 1*os] = x0i - x1i;
    }
}

static inline void
radix2_t1_dit_bwd(
    double * __restrict__ rio_re, double * __restrict__ rio_im,
    const double * __restrict__ W_re, const double * __restrict__ W_im,
    size_t ios, size_t mb, size_t me, size_t ms)
{
    for (size_t m = mb; m < me; m++) {
        const double x0r = rio_re[m*ms + 0*ios], x0i = rio_im[m*ms + 0*ios];
        const double r1r = rio_re[m*ms + 1*ios], r1i = rio_im[m*ms + 1*ios];
        const double wr = W_re[0*me + m], wi = W_im[0*me + m];
        const double x1r = r1r*wr + r1i*wi, x1i = r1i*wr - r1r*wi;
        rio_re[m*ms + 0*ios] = x0r + x1r; rio_im[m*ms + 0*ios] = x0i + x1i;
        rio_re[m*ms + 1*ios] = x0r - x1r; rio_im[m*ms + 1*ios] = x0i - x1i;
    }
}

static inline void
radix2_t1_dif_bwd(
    double * __restrict__ rio_re, double * __restrict__ rio_im,
    const double * __restrict__ W_re, const double * __restrict__ W_im,
    size_t ios, size_t mb, size_t me, size_t ms)
{
    for (size_t m = mb; m < me; m++) {
        const double x0r = rio_re[m*ms + 0*ios], x0i = rio_im[m*ms + 0*ios];
        const double x1r = rio_re[m*ms + 1*ios], x1i = rio_im[m*ms + 1*ios];
        const double wr = W_re[0*me + m], wi = W_im[0*me + m];
        const double dr = x0r - x1r, di = x0i - x1i;
        rio_re[m*ms + 0*ios] = x0r + x1r; rio_im[m*ms + 0*ios] = x0i + x1i;
        rio_re[m*ms + 1*ios] = dr*wr + di*wi; rio_im[m*ms + 1*ios] = di*wr - dr*wi;
    }
}

static inline void
radix2_t1_oop_dit_bwd_scalar(
    const double * __restrict__ in_re, const double * __restrict__ in_im,
    double * __restrict__ out_re, double * __restrict__ out_im,
    const double * __restrict__ W_re, const double * __restrict__ W_im,
    size_t is, size_t os, size_t me)
{
    for (size_t m = 0; m < me; m++) {
        const double x0r = in_re[m + 0*is], x0i = in_im[m + 0*is];
        const double r1r = in_re[m + 1*is], r1i = in_im[m + 1*is];
        const double wr = W_re[0*me + m], wi = W_im[0*me + m];
        const double x1r = r1r*wr + r1i*wi, x1i = r1i*wr - r1r*wi;
        out_re[m + 0*os] = x0r + x1r; out_im[m + 0*os] = x0i + x1i;
        out_re[m + 1*os] = x0r - x1r; out_im[m + 1*os] = x0i - x1i;
    }
}

#endif /* FFT_RADIX2_SCALAR_H */
