/* ===================== PROVENANCE =====================
 * Generated by: gen_radix.exe 2 --r2cf --isa avx2 --su --emit-c
 * Env overrides: (none)
 * Family: t1
 * ISA: 256-bit vectors, 16 vector regs (16-reg pressure rules apply)
 * Scheduler: the list scheduler (flag name --su; lazy loads, sink-first, cp_dist, SU tiebreak; section 30)
 * GH pressure mode: false (auto-rule: vec_regs<=16 && n>=32; +4-8% documented)
 * Construction: MONOLITHIC (below blocking threshold, or prime/Direct: no CT pass boundary)
 * Regalloc+pinning: false (gate: log3+avx512+R<=32 OR avx2 n1 R>=16, sections 32/34/35; kill switch VFFT_NO_REGALLOC)
 * Value fences: false (asm-volatile +v stops gcc rematerialization, doc 28; exempt: n1 avx2 R in {8,16}, measured better unfenced)
 * log3: no
 * ====================================================== */
#include <immintrin.h>
#include <stddef.h>

__attribute__((target("avx2,fma")))
void radix2_r2cf_avx2(
    const double * __restrict__ in_re,
    double       * __restrict__ out_re,
    double       * __restrict__ out_im,
    ptrdiff_t is,
    ptrdiff_t os_re,
    ptrdiff_t os_im,
    size_t vl)
{
    size_t v = 0;
    for (; v + 4 <= vl; v += 4) {
        const __m256d t0 = _mm256_loadu_pd(&in_re[1*is + v]);
        const __m256d t3 = _mm256_loadu_pd(&in_re[0*is + v]);
        const __m256d t4 = _mm256_add_pd(t0, t3);
        const __m256d t7 = _mm256_sub_pd(t3, t0);

        _mm256_storeu_pd(&out_re[0*os_re + v], t4);
        _mm256_storeu_pd(&out_re[1*os_re + v], t7);
    }
    if (v < vl) {
        const size_t rem = vl - v;
        if (rem == 1) {
        const double t0 = in_re[1*is + v];
        const double t3 = in_re[0*is + v];
        const double t4 = (t0 + t3);
        const double t7 = (t3 - t0);

        out_re[0*os_re + v] = t4;
        out_re[1*os_re + v] = t7;
        } else {
            for (; v + 2 <= vl; v += 2) {
        const __m128d t0 = _mm_loadu_pd(&in_re[1*is + v]);
        const __m128d t3 = _mm_loadu_pd(&in_re[0*is + v]);
        const __m128d t4 = _mm_add_pd(t0, t3);
        const __m128d t7 = _mm_sub_pd(t3, t0);

        _mm_storeu_pd(&out_re[0*os_re + v], t4);
        _mm_storeu_pd(&out_re[1*os_re + v], t7);
            }
            if (v < vl) {
        const double t0 = in_re[1*is + v];
        const double t3 = in_re[0*is + v];
        const double t4 = (t0 + t3);
        const double t7 = (t3 - t0);

        out_re[0*os_re + v] = t4;
        out_re[1*os_re + v] = t7;
            }
        }
    }
}
/* codelet-metrics [intrinsic, gen-time]:
 *   fp_instr=2  flops=2  (add=1 sub=1 mul=0 fma=0 cmul=0 neg=0)
 *   essential_io=4 ops (vec_loads=2 + stores=2)  [+0 hoisted scalar-twiddle loads, not counted]
 *   cross_pass_cut=0 slots => +0 mem ops, 0 vectors live across pass boundary
 *   memory_floor=4 mem ops   peak_live(max-per-pass)=4   budget=14 regs
 *   ROOFLINE: MEMORY-BOUND at floor (memory_floor > fp_instr)
 *   PRESSURE: fits (peak_live <= budget)
 */
