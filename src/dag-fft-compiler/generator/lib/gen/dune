; vfft_gen -- M9 (§9 library #3): the feature families + driver + corpus.
; Real / C2c_split (emit_engine) / Cascade_z / C2c_il over the kernel
; engine; Codelet = the descriptor; Coverage + Gen_main = driver/corpus
; (M10 inverts Coverage into Corpus).
(library
 (name vfft_gen)
 (wrapped false)
 (libraries vfft_kernel vfft_cx unix))
