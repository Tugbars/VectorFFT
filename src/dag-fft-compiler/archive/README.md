# archive/ — dead trees kept for historical tracking

`old-lib/` — the pre-cx generator monolith snapshot (3 files, 5,305 lines),
UN-CITABLE by standing law ("ALL of old-lib is DEAD — never cite it").
Successors: `generator/lib/codelet_cil.ml` + `generator/lib/cx_*.ml`.
Moved here from `generator/old-lib/` on 2026-08-14 (owner decision, revising
the earlier keep-in-place call) so the generator directory reads clean.
Compiled by nothing; ripgrep skips it via `old-lib/.ignore` (`rg --no-ignore`
reaches it deliberately). Scheduled for deletion at the v1.0 release.
