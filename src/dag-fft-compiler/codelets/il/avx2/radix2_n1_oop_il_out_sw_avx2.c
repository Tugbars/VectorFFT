/* DERIVED CODELET — mechanical IL transform (il_derive.py)
 * source: radix2_n1_oop_avx2.c  mode: il_out_flip  isa: avx2
 * Every out_re/out_im access replaced per-line by the interleave
 * lattice against out_z; arithmetic untouched; widths 512/256/128/scalar
 * all handled. PAIR ORDER (im,re) — bwd-via-swap contract.
 * Regenerate by re-running the tool. */
/* ===================== PROVENANCE =====================
 * Generated by: gen_radix.exe 2 --oop --oop-buffer-oop --oop-load UG --oop-store UG --isa avx2 --emit-c
 * Env overrides: (none)
 * Family: OOP n1 leaf (no twiddles); edges UG (unit-stride across the transform group)/UG (unit-stride across the transform group); buffer OutOfPlace (Bailey v3_t1)
 * ISA: 16 vector regs (16-reg pressure rules apply)
 * Scheduler: shared Pipeline cascade + cluster-sequential emission; Tier C cluster-local SU on the spill path (section 24); monolithic path tag-ordered (Tier 1 queue item, section 25)
 * Tier C uarch: raptor_lake_avx2 (per-ISA selection: GH threshold 12 vs 24; section 24)
 * GH pressure mode: false (auto-rule: vec_regs<=16 && radix>=32)
 * Construction: MONOLITHIC (below blocking threshold, or twiddled/prime path)
 * Value fences: false (Pipeline-computed prep.fence_enabled)
 * Regalloc+pinning: not wired on the OOP path (render-convention blocker, section 36)
 * ====================================================== */
/* Auto-generated by vfft_v2 codelet generator — OOP family (M2). */
#include <immintrin.h>
#include <stddef.h>

__attribute__((target("avx2,fma")))
void radix2_n1_oop_fwd_avx2_UG_UG_il_out_sw(
    const double * __restrict__ in_re,
    const double * __restrict__ in_im,
    double       * __restrict__ out_z,         /* interleaved, (im,re) for bwd-swap */
    double       * __restrict__ out_unused,
    const double * __restrict__ tw_re,
    const double * __restrict__ tw_im,
    size_t in_leg_stride,
    size_t in_group_stride,
    size_t out_leg_stride,
    size_t out_group_stride,
    size_t me)
{
    const __m256i _vfft_il_m_even = _mm256_set_epi64x(0,-1,0,-1);
    const __m256i _vfft_il_m_odd  = _mm256_set_epi64x(-1,0,-1,0);
    (void)_vfft_il_m_even; (void)_vfft_il_m_odd;

    (void)tw_re; (void)tw_im;
    size_t b = 0;
    for (; b + 4 <= me; b += 4) {
        __m256d lane_re_0, lane_im_0;
        __m256d out_lane_re_0, out_lane_im_0;
        __m256d lane_re_1, lane_im_1;
        __m256d out_lane_re_1, out_lane_im_1;

        /* UnitGroup load: vec_width groups are consecutive (stride 1)
           so they load as one SIMD register per leg. R separate
           strided loads populate the R lane registers — no transpose. */
        lane_re_0 = _mm256_loadu_pd(&in_re[b * in_group_stride + 0 * in_leg_stride]);
        lane_im_0 = _mm256_loadu_pd(&in_im[b * in_group_stride + 0 * in_leg_stride]);
        lane_re_1 = _mm256_loadu_pd(&in_re[b * in_group_stride + 1 * in_leg_stride]);
        lane_im_1 = _mm256_loadu_pd(&in_im[b * in_group_stride + 1 * in_leg_stride]);

        /* === BUTTERFLY BODY (monolithic) ===
           Tier A: algsimp cascade + inline + fence, single scope. */
                const __m256d t0 = lane_re_1;
                const __m256d t2 = lane_im_1;
                const __m256d t5 = lane_re_0;
                const __m256d t7 = lane_im_0;
                const __m256d t8 = _mm256_sub_pd(t5, t0);
                const __m256d t10 = _mm256_sub_pd(t7, t2);
                const __m256d t11 = _mm256_add_pd(t0, t5);
                const __m256d t12 = _mm256_add_pd(t2, t7);

        out_lane_re_1 = t8;
        out_lane_im_1 = t10;
        out_lane_re_0 = t11;
        out_lane_im_0 = t12;

        /* UnitGroup store: R separate strided SIMD stores, no transpose. */
        { __m256d _ost1v = out_lane_re_0;
          _mm256_maskstore_pd(&out_z[2*(b * out_group_stride + 0 * out_leg_stride)], _vfft_il_m_odd, _mm256_permute4x64_pd(_ost1v, 0x50));
          _mm256_maskstore_pd(&out_z[2*(b * out_group_stride + 0 * out_leg_stride) + 4], _vfft_il_m_odd, _mm256_permute4x64_pd(_ost1v, 0xFA)); }
        { __m256d _ost2v = out_lane_im_0;
          _mm256_maskstore_pd(&out_z[2*(b * out_group_stride + 0 * out_leg_stride)], _vfft_il_m_even, _mm256_permute4x64_pd(_ost2v, 0x50));
          _mm256_maskstore_pd(&out_z[2*(b * out_group_stride + 0 * out_leg_stride) + 4], _vfft_il_m_even, _mm256_permute4x64_pd(_ost2v, 0xFA)); }
        { __m256d _ost3v = out_lane_re_1;
          _mm256_maskstore_pd(&out_z[2*(b * out_group_stride + 1 * out_leg_stride)], _vfft_il_m_odd, _mm256_permute4x64_pd(_ost3v, 0x50));
          _mm256_maskstore_pd(&out_z[2*(b * out_group_stride + 1 * out_leg_stride) + 4], _vfft_il_m_odd, _mm256_permute4x64_pd(_ost3v, 0xFA)); }
        { __m256d _ost4v = out_lane_im_1;
          _mm256_maskstore_pd(&out_z[2*(b * out_group_stride + 1 * out_leg_stride)], _vfft_il_m_even, _mm256_permute4x64_pd(_ost4v, 0x50));
          _mm256_maskstore_pd(&out_z[2*(b * out_group_stride + 1 * out_leg_stride) + 4], _vfft_il_m_even, _mm256_permute4x64_pd(_ost4v, 0xFA)); }
    }
    if (b < me) {
        const size_t rem = me - b;
        if (rem == 1) {
        double lane_re_0, lane_im_0;
        double out_lane_re_0, out_lane_im_0;
        double lane_re_1, lane_im_1;
        double out_lane_re_1, out_lane_im_1;

        /* UnitGroup load: vec_width groups are consecutive (stride 1)
           so they load as one SIMD register per leg. R separate
           strided loads populate the R lane registers — no transpose. */
        lane_re_0 = in_re[b * in_group_stride + 0 * in_leg_stride];
        lane_im_0 = in_im[b * in_group_stride + 0 * in_leg_stride];
        lane_re_1 = in_re[b * in_group_stride + 1 * in_leg_stride];
        lane_im_1 = in_im[b * in_group_stride + 1 * in_leg_stride];

        /* === BUTTERFLY BODY (monolithic) ===
           Tier A: algsimp cascade + inline + fence, single scope. */
                const double t0 = lane_re_1;
                const double t2 = lane_im_1;
                const double t5 = lane_re_0;
                const double t7 = lane_im_0;
                const double t8 = (t5 - t0);
                const double t10 = (t7 - t2);
                const double t11 = (t0 + t5);
                const double t12 = (t2 + t7);

        out_lane_re_1 = t8;
        out_lane_im_1 = t10;
        out_lane_re_0 = t11;
        out_lane_im_0 = t12;

        /* UnitGroup store: R separate strided SIMD stores, no transpose. */
        out_z[2*(b * out_group_stride + 0 * out_leg_stride) + 1] = out_lane_re_0;
        out_z[2*(b * out_group_stride + 0 * out_leg_stride)] = out_lane_im_0;
        out_z[2*(b * out_group_stride + 1 * out_leg_stride) + 1] = out_lane_re_1;
        out_z[2*(b * out_group_stride + 1 * out_leg_stride)] = out_lane_im_1;
        } else {
            for (; b + 2 <= me; b += 2) {
        __m128d lane_re_0, lane_im_0;
        __m128d out_lane_re_0, out_lane_im_0;
        __m128d lane_re_1, lane_im_1;
        __m128d out_lane_re_1, out_lane_im_1;

        /* UnitGroup load: vec_width groups are consecutive (stride 1)
           so they load as one SIMD register per leg. R separate
           strided loads populate the R lane registers — no transpose. */
        lane_re_0 = _mm_loadu_pd(&in_re[b * in_group_stride + 0 * in_leg_stride]);
        lane_im_0 = _mm_loadu_pd(&in_im[b * in_group_stride + 0 * in_leg_stride]);
        lane_re_1 = _mm_loadu_pd(&in_re[b * in_group_stride + 1 * in_leg_stride]);
        lane_im_1 = _mm_loadu_pd(&in_im[b * in_group_stride + 1 * in_leg_stride]);

        /* === BUTTERFLY BODY (monolithic) ===
           Tier A: algsimp cascade + inline + fence, single scope. */
                const __m128d t0 = lane_re_1;
                const __m128d t2 = lane_im_1;
                const __m128d t5 = lane_re_0;
                const __m128d t7 = lane_im_0;
                const __m128d t8 = _mm_sub_pd(t5, t0);
                const __m128d t10 = _mm_sub_pd(t7, t2);
                const __m128d t11 = _mm_add_pd(t0, t5);
                const __m128d t12 = _mm_add_pd(t2, t7);

        out_lane_re_1 = t8;
        out_lane_im_1 = t10;
        out_lane_re_0 = t11;
        out_lane_im_0 = t12;

        /* UnitGroup store: R separate strided SIMD stores, no transpose. */
        { __m128d _ost9v = out_lane_re_0;
          _mm_storel_pd(&out_z[2*(b * out_group_stride + 0 * out_leg_stride) + 1], _ost9v);
          _mm_storeh_pd(&out_z[2*(b * out_group_stride + 0 * out_leg_stride) + 2 + 1], _ost9v); }
        { __m128d _ost10v = out_lane_im_0;
          _mm_storel_pd(&out_z[2*(b * out_group_stride + 0 * out_leg_stride)], _ost10v);
          _mm_storeh_pd(&out_z[2*(b * out_group_stride + 0 * out_leg_stride) + 2], _ost10v); }
        { __m128d _ost11v = out_lane_re_1;
          _mm_storel_pd(&out_z[2*(b * out_group_stride + 1 * out_leg_stride) + 1], _ost11v);
          _mm_storeh_pd(&out_z[2*(b * out_group_stride + 1 * out_leg_stride) + 2 + 1], _ost11v); }
        { __m128d _ost12v = out_lane_im_1;
          _mm_storel_pd(&out_z[2*(b * out_group_stride + 1 * out_leg_stride)], _ost12v);
          _mm_storeh_pd(&out_z[2*(b * out_group_stride + 1 * out_leg_stride) + 2], _ost12v); }
            }
            if (b < me) {
        double lane_re_0, lane_im_0;
        double out_lane_re_0, out_lane_im_0;
        double lane_re_1, lane_im_1;
        double out_lane_re_1, out_lane_im_1;

        /* UnitGroup load: vec_width groups are consecutive (stride 1)
           so they load as one SIMD register per leg. R separate
           strided loads populate the R lane registers — no transpose. */
        lane_re_0 = in_re[b * in_group_stride + 0 * in_leg_stride];
        lane_im_0 = in_im[b * in_group_stride + 0 * in_leg_stride];
        lane_re_1 = in_re[b * in_group_stride + 1 * in_leg_stride];
        lane_im_1 = in_im[b * in_group_stride + 1 * in_leg_stride];

        /* === BUTTERFLY BODY (monolithic) ===
           Tier A: algsimp cascade + inline + fence, single scope. */
                const double t0 = lane_re_1;
                const double t2 = lane_im_1;
                const double t5 = lane_re_0;
                const double t7 = lane_im_0;
                const double t8 = (t5 - t0);
                const double t10 = (t7 - t2);
                const double t11 = (t0 + t5);
                const double t12 = (t2 + t7);

        out_lane_re_1 = t8;
        out_lane_im_1 = t10;
        out_lane_re_0 = t11;
        out_lane_im_0 = t12;

        /* UnitGroup store: R separate strided SIMD stores, no transpose. */
        out_z[2*(b * out_group_stride + 0 * out_leg_stride) + 1] = out_lane_re_0;
        out_z[2*(b * out_group_stride + 0 * out_leg_stride)] = out_lane_im_0;
        out_z[2*(b * out_group_stride + 1 * out_leg_stride) + 1] = out_lane_re_1;
        out_z[2*(b * out_group_stride + 1 * out_leg_stride)] = out_lane_im_1;
            }
        }
    }
}
