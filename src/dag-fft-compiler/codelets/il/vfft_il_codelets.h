/* vfft_il_codelets.h — derived interleaved-boundary codelets (r64 set).
 * Generated by tools/il_derive.py from the emitted split sources; arithmetic
 * identical to the originals, only the user-memory boundary changes layout.
 *
 * Contracts:
 *   *_il_in    : the z argument is INTERLEAVED pairs (re,im); split side normal.
 *   *_il_out   : the z argument receives INTERLEAVED pairs; split side normal.
 *   *_il_out_sw: fwd math, pair order FLIPPED (im,re) — the bwd-via-swap trick:
 *                caller passes (in_re <- IM, in_im <- RE); the flipped store then
 *                emits correct (re,im) pairs of the INVERSE transform.
 * Validated bit-exact vs (il2sp/sp2il + split originals) on AVX2 + AVX-512,
 * including scalar (avx2) and masked (avx512) tails.  AVX-512 objects need
 * -mbmi2 (pdep pair-mask doubling in the masked tail). */
#ifndef VFFT_IL_CODELETS_H
#define VFFT_IL_CODELETS_H
#include <stddef.h>
#ifdef __cplusplus
extern "C" {
#endif
#define VFFT_IL_OOP_SIG (const double*,const double*,double*,double*,\
    const double*,const double*,size_t,size_t,size_t,size_t,size_t)
void radix64_n1_oop_fwd_avx2_UG_UG_il_in      VFFT_IL_OOP_SIG;
void radix64_n1_oop_fwd_avx2_UG_UG_il_out_sw  VFFT_IL_OOP_SIG;
void radix64_n1_oop_fwd_avx512_UG_UG_il_in    VFFT_IL_OOP_SIG;
void radix64_n1_oop_fwd_avx512_UG_UG_il_out_sw VFFT_IL_OOP_SIG;
void radix64_n1_fwd_avx2_strided_il_out  (const double*,const double*,double*,
    const double*,const double*,size_t,size_t);
void radix64_n1_bwd_avx2_strided_il_in   (const double*,double*,double*,
    const double*,const double*,size_t,size_t);
void radix64_n1_fwd_avx512_strided_il_out(const double*,const double*,double*,
    const double*,const double*,size_t,size_t);
void radix64_n1_bwd_avx512_strided_il_in (const double*,double*,double*,
    const double*,const double*,size_t,size_t);
#ifdef __cplusplus
}
#endif
#endif
