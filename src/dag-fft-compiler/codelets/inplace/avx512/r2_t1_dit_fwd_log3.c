/* ===================== PROVENANCE =====================
 * Generated by: gen_radix.exe 2 --twiddled --in-place --isa avx512 --log3 --emit-c
 * Env overrides: (none)
 * Family: in-place t1 (twiddled CT)
 * ISA: 512-bit vectors, 32 vector regs
 * Scheduler: the list scheduler (flag name --su; lazy loads, sink-first, cp_dist, SU tiebreak; section 30)
 * GH pressure mode: false (auto-rule: vec_regs<=16 && n>=32; +4-8% documented)
 * Construction: MONOLITHIC (below blocking threshold, or prime/Direct: no CT pass boundary)
 * Regalloc+pinning: false (gate: log3+avx512+R<=32 OR avx2 n1 R>=16, sections 32/34/35; kill switch VFFT_NO_REGALLOC)
 * Value fences: false (asm-volatile +v stops gcc rematerialization, doc 28; exempt: n1 avx2 R in {8,16}, measured better unfenced)
 * log3: DIT radix-decomposition ladder variant
 * ====================================================== */
#include <immintrin.h>
#include <stddef.h>

__attribute__((target("avx512f")))
void radix2_t1_dit_log3_fwd_avx512(
    double       * __restrict__ rio_re,
    double       * __restrict__ rio_im,
    const double * __restrict__ tw_re,
    const double * __restrict__ tw_im,
    size_t ios,
    size_t me)
{
    for (size_t k = 0; k < me; k += 8) {
        const __m512d t0 = _mm512_loadu_pd(&rio_re[1*ios + k]);
        const __m512d t1 = _mm512_loadu_pd(&tw_re[0*me + k]);
        const __m512d t2 = _mm512_loadu_pd(&rio_im[1*ios + k]);
        const __m512d t3 = _mm512_loadu_pd(&tw_im[0*me + k]);
        const __m512d t4 = _mm512_fnmadd_pd(t2, t3, _mm512_mul_pd(t0, t1));
        const __m512d t5 = _mm512_fmadd_pd(t0, t3, _mm512_mul_pd(t2, t1));
        const __m512d t9 = _mm512_loadu_pd(&rio_re[0*ios + k]);
        const __m512d t12 = _mm512_sub_pd(t9, t4);
        const __m512d t15 = _mm512_add_pd(t4, t9);
        const __m512d t11 = _mm512_loadu_pd(&rio_im[0*ios + k]);
        const __m512d t14 = _mm512_sub_pd(t11, t5);
        const __m512d t16 = _mm512_add_pd(t5, t11);

        _mm512_storeu_pd(&rio_re[1*ios + k], t12);
        _mm512_storeu_pd(&rio_im[1*ios + k], t14);
        _mm512_storeu_pd(&rio_re[0*ios + k], t15);
        _mm512_storeu_pd(&rio_im[0*ios + k], t16);
    }
}
/* codelet-metrics [intrinsic, gen-time]:
 *   fp_instr=8  flops=8  (add=2 sub=2 mul=0 fma=0 cmul=2 neg=0)
 *   essential_io=10 ops (vec_loads=6 + stores=4)  [+0 hoisted scalar-twiddle loads, not counted]
 *   cross_pass_cut=0 slots => +0 mem ops, 0 vectors live across pass boundary
 *   memory_floor=10 mem ops   peak_live(max-per-pass)=8   budget=28 regs
 *   ROOFLINE: MEMORY-BOUND at floor (memory_floor > fp_instr)
 *   PRESSURE: fits (peak_live <= budget)
 */
