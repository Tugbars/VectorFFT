# Build
/build/
/build-*/
cmake-build-*/
CMakeFiles/
*.exe
*.obj
*.o
*.pdb
*.ilk

# Generated codelets (regenerate via generators/generate_all)
#src/stride-fft/codelets/avx2/*.h
#src/stride-fft/codelets/avx512/*.h
#src/stride-fft/codelets/scalar/*.h

# Benchmark artifacts (but NOT the PNG plots — those are used in README)
src/stride-fft/bench/vfft_wisdom.txt
src/stride-fft/bench/vfft_bench_results.csv

# Old test-codelets build artifacts
src/test-codelets/build_bench/
src/test-codelets.zip

# Generated plots at root (but allow docs/performance/ for README images)
vfft_*.png
!docs/performance/*.png
# Allow committing notebook-generated plots from build_tuned/results/ for README use.
!build_tuned/results/vfft_*.png

# build_tuned/ scratch + run artifacts
build_tuned/**/*.log
build_tuned/vfft_wisdom_tmp.txt

# IDE / tools
/.vscode/
/.vs/
/.cache/
/.claude/
*.code-workspace
*.swp
*~
.idea/

# vcpkg
/vcpkg/

# OS
Thumbs.db
.DS_Store
*.pyc
/src/test-gen/R32/build
/src/codelet-bench
/build_cmake_test
/build_icx
/build_gcc
/build_msvc
/build_check
/build_msvc_ex
/build_tuned/dev/bench_vtune/vt_hotspots
build_tuned/vfft_wisdom_tuned_bluestein.6cells.txt.bak
build_tuned/vfft_wisdom_tuned_bluestein.calib1.bak
/build_tuned/dev/bench_vtune/vt_uarch-exploration
build_tuned/dev/bench_vtune/bench_output_hotspots.txt
build_tuned/dev/bench_vtune/bench_output_uarch-exploration.txt
build_tuned/dev/bench_vtune/bench_output_hotspots.txt
/src/prototype/_build/default
/build_tuned/dev/bench_vtune/vt_cascade_uarch-exploration
src/prototype/docs/59_avx512_strided_test_guide.md
/src/prototype/_build/default
/src/prototype/_build
/src/prototype/_build_bench
/src/prototype/build_tuned
/src/dag-fft-compiler/generator/_raylib
src/dag-fft-compiler/MKLBench/_smoke.txt
/docs/r2c_bundle
/src/dag-fft-compiler/generator/_build
/build_tuned/benches/vt_rfft_mkl
/build_tuned/benches/vt_rfft_ours
build_tuned/benches/mb_256.txt
*.txt

# Generated wisdom files are committed — single source of truth, one folder,
# visible to everyone (spike/rfft/c2r/bluestein/oop). Keep all wisdoms here.
!src/dag-fft-compiler/generator/generated/*.txt

# Refactor-safety baseline artifacts are COMMITTED — they are the reference the
# migration diffs against, so an ignored baseline means the harness reports green
# on nothing. See docs/design/refactor_safety_harness.md.
!src/tools/baseline/reference/
!src/tools/baseline/reference/**

# Schedule-search experiment sandbox — local-only, NEVER committed to the repo.
# Throwaway codelets, benches, and search drivers live here so experiments
# can't leak into production (see docs/roadmap/schedule_search_plan.md).
/src/dag-fft-compiler/experiments/
/build_tuned/benches/mkl_probes
/docs/research
/build_tuned/dev/bench_vtune
/src/dag-fft-compiler/jit/generated
build_tuned/exec_purity_audit.py
docs/roadmap/cascade_natural_inplace_plan.md
build_tuned/benches/smallre_mkl_probe.c
build_tuned/benches/smallre_pairs_probe.c
build_tuned/benches/smallre_split_probe.c

# owner 2026-09-07: design comparison notes kept local
docs/design/cascade_vs_mkl_method.md
docs/design/cascade_zturn_t_plan.md
docs/design/sub2048_campaign_article.md
docs/design/zturn_t_ship_plan.md

# owner design contracts (local, never committed)
docs/design/design_contracts.md
docs/design/pow2_race_pools.md
docs/design/exact_trig_constants.md

# The gauntlet's runs live in gauntlet/results/ (the standing runs are tracked: the
# csv rows are the record; store/*.txt and *.log stay ignored by the rules above).

# The wisdom store lives in src/wisdom/ (2026-09-24): the shards and the per-host
# subtrees are tracked there; the global *.txt ignore above must not swallow them.
!src/wisdom/**/*.txt
# The merge writes a backup beside each shard (gauntlet merge); backups are local, never tracked.
src/wisdom/**/*.bak_pre_merge_*
